# Optional ML, vector search, and training dependencies.
# Install these only when you need local model experiments, vector RAG, or DeepSpeed demos.

torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
transformers>=4.35.0
peft>=0.6.0
datasets>=2.14.0
accelerate>=0.24.0
safetensors>=0.4.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
deepspeed>=0.12.0; platform_system != "Windows"
